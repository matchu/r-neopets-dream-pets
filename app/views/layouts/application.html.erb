<!DOCTYPE html>
<html>
<head>
  <title>/r/neopets Dream Pets</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <% if user_signed_in? %>
    <%= tag 'meta', name: 'current-user-id', content: current_user.id %>
    <%= tag 'meta', name: 'current-user-admin', content: current_user.admin %>
  <% end %>
</head>
<body>

<header>
  <% if user_signed_in? %>
    Welcome, <%= link_to_user current_user %>!
    <% if current_user.admin? %>
      As an admin, you can edit or delete <em>any</em> pet request.
    <% end %>
    <%= link_to 'Sign out', sign_out_path %>
  <% else %>
    <% if Rails.env.development? %>
      <%= link_to 'Sign in as a developer', '/auth/developer' %>
    <% end %>
    <%= link_to 'Sign in with Reddit', '/auth/reddit' %>
  <% end %>
</header>

<% flash.each do |key, value| %>
  <%= content_tag :div, value, class: "flash-#{key}" %>
<% end %>

<%= yield %>

<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>
