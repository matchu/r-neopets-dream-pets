<%= form_for(@pet_request) do |f| %>
  <% if @pet_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pet_request.errors.count, "error") %> prohibited this pet_request from being saved:</h2>

      <ul>
      <% @pet_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :species_id %><br>
    <%= f.collection_select :species_id, @species, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :color_id %><br>
    <%= f.collection_select :color_id, @colors, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :conversion_state_id %><br>
    <%= f.collection_select :conversion_state_id, @conversion_states, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :minimum_name_quality_id %><br>
    <%= f.collection_select :minimum_name_quality_id, @name_qualities, :id, :abbreviation %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="field">
    <%= f.label :requires_clothes %><br>
    <%= f.check_box :requires_clothes %>
  </div>
  <div class="actions">
    <%= f.submit 'Save' %>
  </div>
<% end %>
